# ============================================================
# 钢铁表面缺陷检测系统 - 项目依赖
# Python 3.8+ | CUDA 11.x | Windows 10/11
# ============================================================

# --- 深度学习框架 ---
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# --- 计算机视觉 ---
opencv-python>=4.8.0
Pillow>=10.0.0

# --- Web 工作台 ---
gradio>=4.0.0

# --- 数据处理 ---
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# --- 推理加速 (可选) ---
onnx>=1.14.0
onnxruntime-gpu>=1.15.0
# tensorrt  # 需手动安装，对应 CUDA 版本
# pycuda    # 需手动安装，对应 CUDA 版本

# --- 两阶段筛查与 SAHI ---
scipy>=1.10.0  # FastScreener FFT 频域分析
# sahi         # 可选: 官方 SAHI 库 (本系统自带实现)

# --- 工具库 ---
PyYAML>=6.0
python-dotenv>=1.0.0
tqdm>=4.65.0
loguru>=0.7.0
psutil>=5.9.0

# --- 工业通信 ---
pymodbus>=3.6.0

# --- OpenClaw Agent (可选) ---
# openclaw  # 按需安装

# --- 测试 ---
pytest>=7.4.0
pytest-cov>=4.1.0

# --- 代码质量 ---
ruff>=0.1.0
